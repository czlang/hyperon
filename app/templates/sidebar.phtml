	{foreach $beeps as $beep}
		<div class="beep">
			
			{!$beep->body|texy}

			<p class="info">{!$beep->date|timeAgoInWords} napsal {$beep->username}</p>

			<div>
				tagy:
				{foreach $presenter->getPostTags($beep->id) as $tag}
					<a href="{plink Posts:tag, $tag->tag_url}">{$tag->tag}</a>
				{/foreach}
			</div>	

		</div>

	{/foreach}


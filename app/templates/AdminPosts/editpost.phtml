{block #content}

<h2>Úprava novinky <a href="{plink Posts:post, $post_info->url}">{$post_info->title}</a></h2>

	<div id="newpost-form">
		{widget postForm}
	</div>




<p id="all-tags">Tagy:
	{foreach $tags as $tag}
		<a href="#" onclick="return $('#frmpostForm-tags').addTag({$tag->tag});">{$tag->tag}</a>
	{/foreach}
</p>

 
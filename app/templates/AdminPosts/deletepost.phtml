{block #content}


<h2>Delete Post</h2>

{if $post}
<p>REALLY delete ‘{if ($post->title)} {$post->title} {else} {$post->body|truncate:50} {/if}’?</p>
{widget deletePostForm}

{else}
<p>Sorry, but can't find this.</p>
{/if}

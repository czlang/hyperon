{block content}
	
		<h1>{$post->title}</h1>
		<p>{!$post->date|timeAgoInWords}</p>
		{!$post->body|texy}
		<p>napsal {$post->username}</p>

		<div>
			tagy:
			{foreach $presenter->getPostTags($post->id) as $tag}
				<a href="{plink Posts:tag, $tag->tag_url}">{$tag->tag}</a>
			{/foreach}
		</div>

	

